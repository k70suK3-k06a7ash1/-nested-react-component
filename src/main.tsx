import React from "react";
import ReactDOM from "react-dom/client";
import "./index.css";
import CategoryManager from "./components/sample.tsx";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <CategoryManager />
  </React.StrictMode>
);
